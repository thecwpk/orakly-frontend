/**
 * Client stores (Zustand). Prefer `@/store/<domain>` for new slices.
 * Legacy imports may still resolve `@/state/*` until migrated.
 */
export { useAppShellStore } from "@/widgets/app-shell/store/use-app-shell-store";
