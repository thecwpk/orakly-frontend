/**
 * App-composition route map — use shared `ROUTES` as canonical paths.
 * Import from here in `src/app` (layouts, navigation shells) for consistency.
 */
export { ROUTES as APP_ROUTES } from "@/shared/constants/routes";
