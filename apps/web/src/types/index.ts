/** Web workspace types — extend `@orakly/types`; keep transport/client-only types here. */
export type { ApiErr, ApiOk, ApiResult } from "@/api/types";
export type { Market, MarketOutcome, MarketStatus } from "@orakly/types";
