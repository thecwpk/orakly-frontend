{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run db:generate && npm run build --workspace=web",
  "crons": [
    {
      "path": "/api/internal/cron/sample-market-odds-snapshots",
      "schedule": "0 12 * * *"
    }
  ]
}
