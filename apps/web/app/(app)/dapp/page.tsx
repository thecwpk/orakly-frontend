import { DappHubPage } from "@/widgets/dapp-hub/dapp-hub-page";

export const metadata = { title: "Orakly — Crypto Attention Market" };

export default function DappPage() {
  return <DappHubPage />;
}
